name: Pinned Actions
on:
  pull_request:
    branches:
      - master
      
jobs:
  pinned_actions:
    name: Pin GitHub Actions to SHA
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Ensure SHA pinned actions
        uses: philips-forks/github-actions-ensure-sha-pinned-actions@v1.0.0
        
